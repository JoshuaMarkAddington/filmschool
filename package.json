{
  "name": "filmschool",
  "version": "1.0.0",
  "private": true,
  "description": "Adders Film School website — Cloudflare Worker + static assets",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
